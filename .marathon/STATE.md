# Marathon State — claude-code-rlm

## Last completed

**Phase 4, Unit 4 (extractPriorBlocksFromTranscript + wire into gatherConversationContext) — commit 18baf55.** Added `extractPriorBlocksFromTranscript(transcriptText, {window=CONFIG.contextWindow})` right after `extractPriorRLMBlocks` in the Phase-4 section of `rlm-hook.mjs` — the JSONL-aware wrapper the Unit-1 contract demanded. It splits the raw transcript into lines, `JSON.parse`es each (skips blanks/unparseable), decodes that record's FULL message text (`entry.message?.content ?? entry.content`; array-of-blocks → join text blocks, string → as-is), and calls the pure `extractPriorRLMBlocks(text, {window:Infinity, ts})` **per record** with that record's own timestamp. **ts derivation:** numeric epoch passed through; ISO string → `Date.parse` (NaN → undefined → extractor stamps `ts:null` → computeChangedFiles fail-safe treats files as changed). **Does NOT filter by `entry.type`** — the hook's injected `<rlm_preresearch>` can land under a user turn, assistant turn, or synthetic record depending on Claude Code internals, so it scans every parseable record's text content (broader than `gatherConversationContext`'s user/assistant preview filter, deliberately). Accumulates chronological then reverses ONCE to newest-first (re-reverses each record's newest-first output to chronological before appending, correct even for the rare multi-block-per-record case); window caps the COMBINED result (≈ "last N turns' blocks", ≤1 block/turn). Pure, never throws.

**Wired `CONFIG.contextWindow` into `gatherConversationContext`** (the Unit-4 mandate): default param changed `maxMessages = 5` → `maxMessages = CONFIG.contextWindow`; it now reads the file once and returns **`{ messages, priorBlocks }`** (was a bare messages array) — `messages` is the unchanged truncated-preview tail; `priorBlocks = extractPriorBlocksFromTranscript(content, {window: maxMessages})` over the FULL content (not the 200-char previews, which can't hold a block). Returns `null` only when no readable transcript. **Updated the one consumer** `buildRLMPrompt`: it now pulls `conversationContext.messages`, but tolerates BOTH the new `{messages}` object and a legacy bare array (`Array.isArray(conversationContext) ? … : conversationContext?.messages`) so nothing downstream breaks. `main` needed no change (it just threads the value through). Integration suite still 15/15 green (shape change verified safe).

Group 22: 12 inlined faithful-copy tests via `extractPriorBlocksFromTranscript_copy` (reusing Group 19's `extractPriorRLMBlocks_copy` + `preBlock`) plus a `jsonlLine(type,content,timestamp)` builder: single-record extract + ts stamp from ISO; missing/unparseable ts ⇒ null; numeric epoch passthrough; newest-first across records; window cap keeps newest; window≤0/Infinity ⇒ all; skip unparseable/blank/empty-text records; array-content decode (non-text blocks ignored); type-agnostic extraction (`system` record); no-block records ⇒ []; empty/null/undefined/number ⇒ []; `<rlm_analysis>` recognized. 225 unit tests green (was 213).

(Prior — Phase 4, Unit 3 (mtime tracking + block `ts` plumbing) — commit 2a40ed2.) Added `computeChangedFiles(relevantFiles, sinceMs, {cwd=process.cwd(), statImpl=stat})` right after `findReusablePriorBlock` in the "CONVERSATION CONTEXT AWARENESS (Phase 4)" section of `rlm-hook.mjs`. Pure aside from the injected `statImpl` (defaults to fs/promises `stat`); never throws. Returns the subset of input paths considered **changed**: a path is changed iff stat throws (ENOENT/unreadable/deleted/moved) OR `st.mtimeMs > sinceMs` OR stat returns no usable `mtimeMs`. mtime **==** sinceMs ⇒ unchanged (strict `>`). Returns the **original** path strings (not resolved absolutes) so they match what `findReusablePriorBlock`'s `changed.has(f)` looks up. De-dupes (each path reported at most once). Resolves relative paths via `resolve(cwd, f)`, leaves absolutes alone (`isAbsolute`). Drops non-string/empty entries before statting. **Fail-safe:** if `sinceMs` is not a finite number, returns ALL (deduped) input paths as changed — forces re-exploration rather than reuse of unknown-age context.

**RESOLVED the Phase-4 "prior blocks carry NO timestamp" open question — option (a).** Extended `extractPriorRLMBlocks(text, {window, ts})`: it now accepts an optional `ts` opt and stamps every returned block with `ts` (default `null` when absent/non-finite/non-number). Each block's return shape is now `{intent, relevant_files, ts, raw}`. **CONTRACT (in JSDoc):** the block lives in a JSONL line that carries its own `timestamp`; rather than teach the pure-text extractor to re-parse JSONL, the **caller** passes that line's timestamp as `ts`. For accurate PER-BLOCK timestamps the Unit-4 wiring must call the extractor **once per transcript message** with that message's `Date.parse(entry.timestamp)`; calling it on the whole transcript blob stamps every block with the same `ts`. `ts:null` ⇒ `computeChangedFiles` sees non-finite `sinceMs` ⇒ treats all files as changed (fail-safe). This finalizes the return shape so Unit 4 is pure wiring.

Group 21: 10 inlined faithful-copy tests (unchanged mtime≤since ⇒ not changed; modified mtime>since ⇒ changed; missing stat-throws ⇒ changed; mixed subset preserves original paths; relative resolved-under-cwd vs absolute-untouched; empty/non-array/all-empty ⇒ []; non-finite sinceMs ⇒ all deduped paths changed; dedupe repeated paths; stat w/o mtimeMs ⇒ changed; non-string entries dropped) via `fakeStat({path→mtimeMs})` injection — no real FS. Plus 1 `ts`-stamping assertion added to Group 19 (provided ts stamped on every block; absent/Infinity/non-number ⇒ null). 213 unit tests green (was 202).

(Prior — Phase 4, Unit 2 (intent-overlap early-exit helpers) — commit 4e70c13.) Added three pure helpers right after `extractPriorRLMBlocks` in the "CONVERSATION CONTEXT AWARENESS (Phase 4)" section of `rlm-hook.mjs`:
- `classifyIntentLocal(userMessage)` → one of `code_writing|debugging|refactoring|architecture|learning|other`. **DECISION on the Phase-4 open question** ("where does current intent come from BEFORE Haiku?"): **option (a)** — a cheap keyword classifier on the raw user message, so the early-exit can fire on the *current* turn (not just a later one). Driven by module-level `INTENT_KEYWORDS` (ordered `[label, regex]` list); first match wins; order = signal strength: **debugging → refactoring → code_writing → architecture → learning**, else `other`. Labels come from the **agentic-mode enum** (default mode, superset of fast's) so output can match prior blocks from either mode. Misclassification is *safe*: only ever a MISSED reuse (falls through to running Haiku), never a wrong reuse, because reuse also requires unchanged files.
- `intentsOverlap(a, b)` → strict case-insensitive equality of trimmed labels; null/empty/non-string either side ⇒ false. Strictness deliberate (plan warns against stale-context injection).
- `findReusablePriorBlock(currentIntent, priorBlocks, {changedFiles=[]})` → most-recent reusable prior block, or null. priorBlocks is newest-first (as `extractPriorRLMBlocks` returns) so first qualifying = most recent. Qualifies iff: intent overlaps AND intent !== `other` (catch-all too low-confidence) AND ≥1 recorded file AND none of its files ∈ changedFiles. `changedFiles` supplied by caller (Unit 3 computes via mtime); absent ⇒ nothing changed. Pure, never throws; tolerates malformed (null/string/number) entries in the priors array.

Group 20: 19 inlined faithful-copy tests (classifier per-label + precedence + empty/non-string fallback; overlap identical/divergent/null; reuse hit on unchanged files; no-reuse on divergent intent / changed referenced file; still-reuse on unrelated changed file; empty/null priors → null; `other` excluded; file-less block excluded; skip-changed-newer-reuse-older; malformed-entry tolerance). 202 unit tests green (was 183).

(Prior — Phase 4, Unit 1: extract prior RLM blocks from transcript — commit 8206a28.) Added `extractPriorRLMBlocks(transcriptText, {window=CONFIG.contextWindow})` plus two normalizers in a new "CONVERSATION CONTEXT AWARENESS (Phase 4)" section right after `gatherConversationContext` in `rlm-hook.mjs`. **Verified the real emitted tag first:** `formatOutput` wraps a `JSON.stringify(analysis, null, 2)` body in `<rlm_preresearch>` (agentic, the default — has `intent` string + `relevant_files`) or `<rlm_analysis>` (fast: `intent` string + `files`; detailed: `intent.primary`). The extractor is **pure string-in→array-out**: global regex `/<rlm_(?:preresearch|analysis)>([\s\S]*?)<\/...>/g`, `JSON.parse` each body (skip on throw), skip non-object/array roots, push `{intent, relevant_files, raw}`. `normalizeIntent` handles string-or-`{primary}`; `normalizeRelevantFiles` flattens `relevant_files ?? files` accepting strings or `{path}` objects, dropping empties. Matches arrive oldest→newest so the array is **reversed to newest-first** then `slice(0, window)` (window≤0/non-finite ⇒ all). **Never throws.** **CRITICAL caller contract (documented in the JSDoc, matters for Unit 2/3 wiring):** pass DECODED message text, not raw JSONL bytes — inside a JSONL line the block's quotes/newlines are escaped, so `gatherConversationContext`'s existing per-line `JSON.parse` + content-decode must feed the extractor (and note its current `preview` is truncated to 200 chars — too short to hold a full block; the wiring will need the full decoded text, not `preview`). Also added `CONFIG.contextWindow` (`RLM_CONTEXT_WINDOW`, default 5) now since the function default needs it — **this folds in Unit 4's env var early**, so Unit 4 reduces to wiring the window into the look-back, not adding the flag. Group 19: 12 inlined tests (single agentic block w/ string+object files; newest-first ordering; window cap; window≤0/Infinity ⇒ all; `<rlm_analysis>` recognized; detailed `{primary}` intent; malformed body skipped but valid kept; array-root skipped; plain conversation ⇒ []; null/undefined/empty/number ⇒ []; missing intent/files degrades; empty/objectless path entries dropped). 183 unit tests green (was 171).

(Prior — Phase 3, Unit 5 `index.json` reverse index + Unit 6 test coverage — commit ce945e6. PHASE 3 COMPLETE.) Added three helpers to the SEMANTIC CACHE section (after `embeddingPath`, before `saveCacheEmbedding`): `indexPath()` → `<cacheDir>/index.json`; `readCacheIndex()` → parses index.json, returns `{}` on absent/malformed/non-object-or-array root, **never throws**; `updateCacheIndex(key, vec)` → best-effort upsert `index[key] = { dim, vec: Array.from(vec) }`, atomic tmp+rename, **gated on `CONFIG.semanticCache` (zero I/O off)**, whole body try/catch → false on failure (an index failure must never break the embedding write). **Decision taken: inline vectors** (single-read lookup) over key-list. Wired `await updateCacheIndex(key, vec)` into `saveCacheEmbedding` right after the `.embedding` rename. **Refactored `semanticLookup`** into two extracted scorers each returning `{bestKey,bestScore}|null`: `scoreFromIndex(queryVec, simImpl)` (reads index.json once, `Float32Array.from(entry.vec)`, per-entry try/catch skips foreign-dim, returns null if index empty/no-usable-vector) and `scoreFromFiles(queryVec, simImpl)` (the prior per-file `.embedding` scan, unchanged, the robust fallback). New body: embed query → `best = scoreFromIndex(...) ?? scoreFromFiles(...)` → if null log "no candidate embeddings" → threshold check → `checkImpl(bestKey)` (TTL/parse, gone→miss) → hit. `.embedding` files stay the **source of truth**, so absent/empty/corrupt index degrades to file scan; index just lags-but-self-heals (last-writer-wins race accepted per plan). Group 18: 10 inlined tests via `semanticLookupIndexed` + faithful helper copies (upsert round-trip w/ float32 tolerance; multi-key accumulate; same-key overwrite; off-gating no-write; readCacheIndex {} for absent/malformed/array; index-only nearest with ZERO `.embedding` files present; corrupt index→file-scan fallback; absent index→file-scan fallback; below-threshold miss; foreign-dim index entry skipped). **Unit 6 satisfied by existing tests:** similarity-math=Group 15, threshold-gating=Groups 17+18, index-read/write=Group 18. 171 unit tests green (was 161).

(Prior — Phase 3 Unit 4, commit dd02deb: `semanticLookup` file-scan on SHA-256 miss, zero-I/O when flag/key off, per-file + top-level try/catch, wired into `main` after SHA miss, Group 17 8 tests. Unit 3 5c76192: `embeddingPath`/`saveCacheEmbedding` raw-float32 sidecar, Group 16 6 tests. Unit 2 9c6030f: `cosineSimilarity`, length-mismatch THROWS, zero-mag→0, Group 15 7 tests. Unit 1 af5908f: 5 CONFIG flags + `embedText`→Float32Array, Group 14 8 tests. Phase 2 COMPLETE abe7313. Phase 1 COMPLETE.)

## Current phase

Phase 4 — Conversation Context Awareness (Units 1–4 done; Unit 5 remains)

## Next unit

**Phase 4, Unit 5 (final): wire the conversation-aware early-exit into `main`.** All the pieces now exist and are tested — this unit is the integration that makes the optimization actually fire, plus its tests. In `main()` (rlm-hook.mjs ~line 1513), AFTER the cache/semantic-cache misses and AFTER `gatherConversationContext` returns `{messages, priorBlocks}` (line ~1569), and BEFORE invoking Haiku, add the early-exit:
1. `const currentIntent = classifyIntentLocal(userMessage)` (use the un-truncated `userMessage`, not `truncatedMessage`).
2. `const priorBlocks = conversationContext?.priorBlocks ?? []`.
3. For each candidate prior block compute its changed files: `computeChangedFiles(block.relevant_files, block.ts, {cwd: cwd || process.cwd()})`. NOTE `findReusablePriorBlock` takes a SINGLE flat `changedFiles` set, but each block has its OWN `ts`/`sinceMs` — so either (a) call `computeChangedFiles` per block and filter inline, or (b) union the changed sets. Cleanest: iterate priorBlocks newest-first yourself, and for the first block whose intent overlaps (`intentsOverlap(currentIntent, block.intent)`, intent≠`other`, ≥1 file) AND `computeChangedFiles(block.relevant_files, block.ts, {cwd}) ` returns `[]` (nothing changed) → REUSE. (You can still use `findReusablePriorBlock` by passing it that one block's changed set, but the per-block `ts` makes a hand-rolled loop simpler — decide and note it.)
4. On a reuse hit: emit a lightweight **"continuing from prior analysis"** block via `console.log(formatOutput(...))` — reuse the prior block's `raw` analysis (it already has the right shape for `formatOutput`) OR synthesize a minimal `{intent, relevant_files, summary:"Continuing from prior pre-research; files unchanged since last turn."}`. Gate behind `CONFIG.gatherContext` (early-exit only makes sense when context gathering is on). `await log('Phase 4 early-exit: reusing prior analysis (...)')`, then `process.exit(0)` — skip Haiku entirely. This is the "eliminate ~80% re-exploration" payoff (plan Phase 4).
5. **Fail-safe:** the whole early-exit must be best-effort — wrap in try/catch (or rely on the pure helpers never throwing) so a bug here can NEVER block the user; on any doubt fall through to normal Haiku exploration.
6. Tests: this path lives inside `main()` which auto-runs on import, so it can't be unit-tested by importing. Add a Group 23 of inlined faithful copies that simulate the decision: given a fake `{priorBlocks}` + current intent + injected `statImpl`/`sinceMs`, assert the reuse-vs-explore decision and the emitted block shape. Mirror the Group 20/22 style. Consider also an integration-test scenario (test/integration.test.mjs) feeding a transcript with a prior `<rlm_preresearch>` block + unchanged files and asserting the hook short-circuits without spawning Haiku — bonus, gated by RUN_INTEGRATION_TESTS.

After Unit 5, Phase 4 is COMPLETE → move to **Phase 5 — Metrics Dashboard** (plan docs/plan/plan.md ~line 146; Units: metrics JSONL append at hook exit → bench/parse-log.mjs aggregation → bench/dashboard.mjs static HTML + --serve → tests). Read plan Phase 4/5 before starting.

## Known issues / blockers

None. Note: tests inline faithful copies of hook logic (no imports from `rlm-hook.mjs`, since it calls `main()` on load) — keep new SDK tests in that style. The loop runner writes to an untracked nested `.marathon/.marathon/logs/` dir; leave it untracked, do not commit it.

## Phase completion

- [x] Phase 1 — Core Hook (COMPLETE)
- [x] Phase 2 — SDK-Direct Mode (COMPLETE)
- [x] Phase 3 — Semantic Caching (COMPLETE — Units 1-6)
- [~] Phase 4 — Conversation Context Awareness (Units 1–4 done: extractPriorRLMBlocks + CONFIG.contextWindow; classifyIntentLocal + intentsOverlap + findReusablePriorBlock; computeChangedFiles + block `ts`; extractPriorBlocksFromTranscript + gatherConversationContext `{messages, priorBlocks}`. Unit 5 — main() early-exit wiring — remains.)
- [ ] Phase 5 — Metrics Dashboard
