# Marathon State — claude-code-rlm

## Last completed

**Phase 5, Unit 3: `bench/dashboard.mjs` — static HTML render + `--serve` — commit b6f5ff1.** New dependency-free ESM module under `bench/` (Node built-ins only: `http`, `fs/promises`, `url`). Imports the data layer from `parse-log.mjs` (`aggregate`, `parseLog`, `readLog`, `defaultLogPath`) — aggregation is NOT re-implemented. Two responsibilities:
1. **Pure `renderHTML(agg, {logPath})`** — full standalone HTML doc from the `aggregate()` shape. Overall **summary cards** (events, hit/skip/error % + p50/p95/p99), a **per-UTC-day table** (one `<tr>` per `agg.days[]`: day, events, hit%, skip%, err%, p50/p95/p99), and **skip-reasons + events + modes** breakdown tables (from `overall.skip_reasons`/`events`/`modes`, sorted desc by count, empty-state row when absent). Helpers: `esc()` (exported — escapes `& < > "`, coerces null/undefined→''), `pct()` (rate→`%`, null→`—`), `num()` (null→`—`), `mapRows()`. Inline `<style>` block (dark theme, no external CSS/JS). **Defensive:** tolerates null/partial `agg` (`agg?.overall ?? {}`, `Array.isArray(agg?.days)`) — empty/missing renders without throwing. **All interpolated text escaped** (XSS-safe: a `<script>` skip reason renders inert). Null latencies render as `—`, never the string `null`. Pure — no FS/clock/socket, so Group 26 asserts on markup directly.
2. **CLI + `--serve`** behind `import.meta.url === pathToFileURL(process.argv[1]).href`. `parseArgs` handles `[logpath]`, `--out <path>`, `--serve`, `--port N`. Default: `buildHTML(logPath)` (read→parse→aggregate→render) → write to `bench/results/dashboard.html` (or `--out`), print path. `--serve`: `http.createServer` on **port 9876** (`--port` override), **re-reads+re-aggregates the live log per GET request** so it reflects current state, `Content-Type: text/html; charset=utf-8`, 500+plain-text on render error; logs `http://localhost:<port>` to stderr. Server code is OUT of any export — tests never bind a socket. **Smoke-tested both paths:** one-shot render (3582 bytes, day row + 33.3% rates + skip reason present) and `--serve` (HTTP 200, correct content-type, title in body).

Group 26: 10 tests **imported directly** from `../bench/dashboard.mjs` (`renderHTML`/`esc`, side-effect-free on import like parse-log): esc escaping + null/number coercion; full-doc shape (`<!doctype>`…`</html>`, title, inline `<style>`); summary cards w/ %; per-day row per `agg.days[]`; **null-latency `—` not `null`**; skip-reason + mode breakdown rows; **XSS-escape** of a `<script>` skip reason; empty-agg renders w/o throw; null/`{}`/partial-agg tolerance; log-path subheader. **272 unit tests green (was 262);** integration unchanged (dashboard not on hook path).

(Prior — Phase 5, Unit 2: `bench/parse-log.mjs` — daily metrics aggregator — commit 7902d1c.) New dependency-free ESM module under `bench/`. Pure exports (no FS, no clock, directly importable): `parseLog(text)` (JSONL → records; skips blank/unparseable lines and non-object roots, never throws, tolerates non-string input), `aggregate(records)` (per-UTC-day buckets + `overall` rollup), `summarize(records)` (one stat block, reused for both per-day and overall), `percentile(values, p)` (nearest-rank: `idx = clamp(ceil(p/100·N),1,N)−1`; drops non-finite entries; **null on empty input** — documented convention). Each stat block: `total`, `hits`/`hit_rate`, `skips`/`skip_rate`/`skip_reasons` (per-reason map; missing reason ⇒ `unknown`), `errors`/`error_rate`, `events` & `modes` tallies, and `latency:{count,p50,p95,p99,min,max}`. **Hit rate is off the boolean `cache_hit` field (true for cache_hit + context_reuse), NOT the event name** — matches Unit 1's taxonomy. Records lacking a finite numeric `ts` are DROPPED from both days and overall (can't bucket; hook always stamps ts). Days sorted ascending; overall carries `day:'all'`. FS/CLI side helpers (`defaultLogPath()` honoring `RLM_METRICS_FILE`+tilde, `readLog(path)` returns '' on missing, `renderHuman`) — **CLI `main()` is behind `import.meta.url === pathToFileURL(process.argv[1]).href`** so importing has zero side effects (the reason this module can be imported in tests, unlike `rlm-hook.mjs`). CLI accepts `[path]`, `--json`. Smoke-tested: 5/garbage-skipped parse, no-ts drop, 0.25 rates, nearest-rank p50/p99, empty-file clean render.

Group 25: 18 tests **imported directly** from `../bench/parse-log.mjs` (first test group to import real exports rather than inline copies — safe because no `main()`-on-import side effect): percentile nearest-rank (p0/p50/p95/p99/p100, single value, empty⇒null, non-finite dropped); parseLog blank/garbage/non-object-root/non-string tolerance; per-UTC-day bucketing + ascending sort + overall; no-ts/non-finite/string-ts/non-object dropped from days AND overall; hit rate off boolean incl context_reuse; skip rate + reason breakdown + `unknown` default; error rate; latency dist over present `latency_ms` (N=10 p50=50/p95=100/p99=100, latency-less excluded, empty⇒null block); summarize event+mode tallies; aggregate non-array tolerance. **262 unit tests green (was 244);** integration unchanged (15/15, parse-log not on hook path).

(Prior — Phase 5, Unit 1: metrics JSONL append at every `main()` exit — commit 3bfe54b.) Added a `METRICS (Phase 5)` section right after `log()` in `rlm-hook.mjs` with two helpers: `currentMode()` (returns `agentic` if `CONFIG.agenticMode` else `fast`/`detailed` — same precedence the SDK dispatch at line ~1764 and `formatOutput` use) and `appendMetric(metric)` — bullet-proof exactly like `log()`: builds `JSON.stringify({ ts: Date.now(), ...metric }) + '\n'`, `mkdir(...,{recursive})` the share dir, `writeFile(CONFIG.metricsFile, line, {flag:'a'})`, **whole body in try/catch that swallows every error** (a metrics failure must never change exit behavior). New `CONFIG.metricsFile` = `RLM_METRICS_FILE` || `~/.local/share/rlm-hook/metrics.jsonl` (same dir family as `logFile`). **`ts` stamped at write time** (not by caller). Fields spread AFTER ts so a caller can't clobber it. Dependency-free.

**Wired into `main()`:** hoisted `const start = Date.now()` and `let userMessage = ''` ABOVE the `try` (so latency_ms covers the whole invocation incl. stdin read, and the error catch can still read `input_len`); changed the inner `let userMessage = input` → `userMessage = input`. Added a local `recordMetric(event, cache_hit, extra={})` sugar that calls `appendMetric({event, latency_ms: Date.now()-start, mode: currentMode(), input_len: userMessage.length, cache_hit, ...extra})`. Call sites at EVERY exit: `skip` (+reason), SHA `cache_hit`/cache_hit:true (+source:'sha'), semantic `cache_hit`/true (+source:'semantic'), Phase 4 `context_reuse`/true (+intent), `haiku_skip` (+reason), `complete` (normal fresh-analysis finish, cache_hit:false), `error` (+reason) in the catch. Events deliberately go beyond the plan's `cache_hit|cache_miss|skip|error` enum (distinct `semantic`/`context_reuse`/`haiku_skip`/`complete`) so Unit 2's aggregation can break down hit *source* and skip *reason*; the `cache_hit` boolean stays the canonical hit flag.

Group 24: 6 inlined faithful-copy tests via `appendMetric_copy(metric, {metricsFile, writeImpl, now})` (path + writer + clock seams, since `main()` auto-runs and can't be imported) + `currentMode_copy(cfg)`: line shape + ts-stamped-at-write + all 5 fields round-trip; append-not-overwrite across 3 calls (events+ts ordered); extra-field (reason) passthrough; **write-failure swallowed** (throwing writer ⇒ `doesNotReject` AND no file written); **circular-metric stringify failure swallowed**; currentMode agentic>fast>detailed precedence. 244 unit tests green (was 238); integration 15/15.

(Prior — Phase 4, Unit 5 (final): conversation-aware early-exit wired into `main()` — commit 8b3b371. PHASE 4 COMPLETE.) Added `findReusablePriorBlockWithMtime(currentIntent, priorBlocks, {cwd, statImpl=stat})` right after `computeChangedFiles` — the **mtime-aware sibling** of `findReusablePriorBlock`. The reason it exists: `findReusablePriorBlock` takes ONE flat `changedFiles` set, but each recovered block carries its OWN production timestamp (`block.ts`), so "which files changed since this block" must be computed **per block**. It iterates priorBlocks newest-first and returns the first block where: intent overlaps (`intentsOverlap`) AND intent≠`other` AND ≥1 file AND `computeChangedFiles(files, block.ts, {cwd, statImpl})` returns `[]`. Per-block computeChangedFiles is wrapped in try/catch (a block that can't be judged is skipped, not thrown on). `ts:null` ⇒ computeChangedFiles sees non-finite sinceMs ⇒ all files "changed" ⇒ block never reused (fail-safe). **DECISION on the plan's open choice (use findReusablePriorBlock with per-block changed set vs hand-rolled loop): hand-rolled loop**, because the per-block `ts` makes computeChangedFiles a per-iteration call — cleaner than re-deriving a flat set.

**Wired into `main()`** (rlm-hook.mjs, after `gatherConversationContext`/projectContext log, BEFORE the Haiku-invocation block): gated behind `CONFIG.gatherContext`, whole thing in try/catch (best-effort — any failure falls through to normal Haiku exploration, NEVER blocks the user). Classifies `classifyIntentLocal(userMessage)` (the **un-truncated** message, per plan), pulls `conversationContext?.priorBlocks ?? []`, calls the new helper with `cwd: cwd || process.cwd()`. On a hit: builds a **lightly-marked continuation** analysis — shallow-clones `reusable.raw` (already the right shape for `formatOutput`) and prepends `[Continuing from prior pre-research — files unchanged since last turn]` to its `summary` — then `console.log(formatOutput(reusedAnalysis))` + `await log(...)` + `process.exit(0)`, skipping Haiku entirely. This is Phase 4's "eliminate redundant re-exploration" payoff actually firing. Note: `formatOutput` keys off the CURRENT runtime mode (agenticMode/fastMode), which is stable across turns in practice, so the reused raw shape matches.

Group 23: 13 inlined faithful-copy tests via `findReusablePriorBlockWithMtime_copy` + `buildReusedAnalysis_copy` (continuation-marker construction) + `blk()` builder, reusing Group 20/21's `intentsOverlap_copy`/`computeChangedFiles_copy`/`fakeStat`: reuse hit (files unchanged); intent divergence ⇒ null; changed-file miss; **per-block ts window** (newer-but-stale skipped, older-fresh reused — the case a flat changedFiles set can't express); ts:null fail-safe; `other` excluded; file-less excluded; empty/null/non-string inputs; malformed-entry tolerance; throwing-stat skip; continuation-marker shape (with summary / empty summary / null raw). 238 unit tests green (was 225); integration 15/15.

(Prior — Phase 4, Unit 4 (extractPriorBlocksFromTranscript + wire into gatherConversationContext) — commit 18baf55.) Added `extractPriorBlocksFromTranscript(transcriptText, {window=CONFIG.contextWindow})` right after `extractPriorRLMBlocks` in the Phase-4 section of `rlm-hook.mjs` — the JSONL-aware wrapper the Unit-1 contract demanded. It splits the raw transcript into lines, `JSON.parse`es each (skips blanks/unparseable), decodes that record's FULL message text (`entry.message?.content ?? entry.content`; array-of-blocks → join text blocks, string → as-is), and calls the pure `extractPriorRLMBlocks(text, {window:Infinity, ts})` **per record** with that record's own timestamp. **ts derivation:** numeric epoch passed through; ISO string → `Date.parse` (NaN → undefined → extractor stamps `ts:null` → computeChangedFiles fail-safe treats files as changed). **Does NOT filter by `entry.type`** — the hook's injected `<rlm_preresearch>` can land under a user turn, assistant turn, or synthetic record depending on Claude Code internals, so it scans every parseable record's text content (broader than `gatherConversationContext`'s user/assistant preview filter, deliberately). Accumulates chronological then reverses ONCE to newest-first (re-reverses each record's newest-first output to chronological before appending, correct even for the rare multi-block-per-record case); window caps the COMBINED result (≈ "last N turns' blocks", ≤1 block/turn). Pure, never throws.

**Wired `CONFIG.contextWindow` into `gatherConversationContext`** (the Unit-4 mandate): default param changed `maxMessages = 5` → `maxMessages = CONFIG.contextWindow`; it now reads the file once and returns **`{ messages, priorBlocks }`** (was a bare messages array) — `messages` is the unchanged truncated-preview tail; `priorBlocks = extractPriorBlocksFromTranscript(content, {window: maxMessages})` over the FULL content (not the 200-char previews, which can't hold a block). Returns `null` only when no readable transcript. **Updated the one consumer** `buildRLMPrompt`: it now pulls `conversationContext.messages`, but tolerates BOTH the new `{messages}` object and a legacy bare array (`Array.isArray(conversationContext) ? … : conversationContext?.messages`) so nothing downstream breaks. `main` needed no change (it just threads the value through). Integration suite still 15/15 green (shape change verified safe).

Group 22: 12 inlined faithful-copy tests via `extractPriorBlocksFromTranscript_copy` (reusing Group 19's `extractPriorRLMBlocks_copy` + `preBlock`) plus a `jsonlLine(type,content,timestamp)` builder: single-record extract + ts stamp from ISO; missing/unparseable ts ⇒ null; numeric epoch passthrough; newest-first across records; window cap keeps newest; window≤0/Infinity ⇒ all; skip unparseable/blank/empty-text records; array-content decode (non-text blocks ignored); type-agnostic extraction (`system` record); no-block records ⇒ []; empty/null/undefined/number ⇒ []; `<rlm_analysis>` recognized. 225 unit tests green (was 213).

(Prior — Phase 4, Unit 3 (mtime tracking + block `ts` plumbing) — commit 2a40ed2.) Added `computeChangedFiles(relevantFiles, sinceMs, {cwd=process.cwd(), statImpl=stat})` right after `findReusablePriorBlock` in the "CONVERSATION CONTEXT AWARENESS (Phase 4)" section of `rlm-hook.mjs`. Pure aside from the injected `statImpl` (defaults to fs/promises `stat`); never throws. Returns the subset of input paths considered **changed**: a path is changed iff stat throws (ENOENT/unreadable/deleted/moved) OR `st.mtimeMs > sinceMs` OR stat returns no usable `mtimeMs`. mtime **==** sinceMs ⇒ unchanged (strict `>`). Returns the **original** path strings (not resolved absolutes) so they match what `findReusablePriorBlock`'s `changed.has(f)` looks up. De-dupes (each path reported at most once). Resolves relative paths via `resolve(cwd, f)`, leaves absolutes alone (`isAbsolute`). Drops non-string/empty entries before statting. **Fail-safe:** if `sinceMs` is not a finite number, returns ALL (deduped) input paths as changed — forces re-exploration rather than reuse of unknown-age context.

**RESOLVED the Phase-4 "prior blocks carry NO timestamp" open question — option (a).** Extended `extractPriorRLMBlocks(text, {window, ts})`: it now accepts an optional `ts` opt and stamps every returned block with `ts` (default `null` when absent/non-finite/non-number). Each block's return shape is now `{intent, relevant_files, ts, raw}`. **CONTRACT (in JSDoc):** the block lives in a JSONL line that carries its own `timestamp`; rather than teach the pure-text extractor to re-parse JSONL, the **caller** passes that line's timestamp as `ts`. For accurate PER-BLOCK timestamps the Unit-4 wiring must call the extractor **once per transcript message** with that message's `Date.parse(entry.timestamp)`; calling it on the whole transcript blob stamps every block with the same `ts`. `ts:null` ⇒ `computeChangedFiles` sees non-finite `sinceMs` ⇒ treats all files as changed (fail-safe). This finalizes the return shape so Unit 4 is pure wiring.

Group 21: 10 inlined faithful-copy tests (unchanged mtime≤since ⇒ not changed; modified mtime>since ⇒ changed; missing stat-throws ⇒ changed; mixed subset preserves original paths; relative resolved-under-cwd vs absolute-untouched; empty/non-array/all-empty ⇒ []; non-finite sinceMs ⇒ all deduped paths changed; dedupe repeated paths; stat w/o mtimeMs ⇒ changed; non-string entries dropped) via `fakeStat({path→mtimeMs})` injection — no real FS. Plus 1 `ts`-stamping assertion added to Group 19 (provided ts stamped on every block; absent/Infinity/non-number ⇒ null). 213 unit tests green (was 202).

(Prior — Phase 4, Unit 2 (intent-overlap early-exit helpers) — commit 4e70c13.) Added three pure helpers right after `extractPriorRLMBlocks` in the "CONVERSATION CONTEXT AWARENESS (Phase 4)" section of `rlm-hook.mjs`:
- `classifyIntentLocal(userMessage)` → one of `code_writing|debugging|refactoring|architecture|learning|other`. **DECISION on the Phase-4 open question** ("where does current intent come from BEFORE Haiku?"): **option (a)** — a cheap keyword classifier on the raw user message, so the early-exit can fire on the *current* turn (not just a later one). Driven by module-level `INTENT_KEYWORDS` (ordered `[label, regex]` list); first match wins; order = signal strength: **debugging → refactoring → code_writing → architecture → learning**, else `other`. Labels come from the **agentic-mode enum** (default mode, superset of fast's) so output can match prior blocks from either mode. Misclassification is *safe*: only ever a MISSED reuse (falls through to running Haiku), never a wrong reuse, because reuse also requires unchanged files.
- `intentsOverlap(a, b)` → strict case-insensitive equality of trimmed labels; null/empty/non-string either side ⇒ false. Strictness deliberate (plan warns against stale-context injection).
- `findReusablePriorBlock(currentIntent, priorBlocks, {changedFiles=[]})` → most-recent reusable prior block, or null. priorBlocks is newest-first (as `extractPriorRLMBlocks` returns) so first qualifying = most recent. Qualifies iff: intent overlaps AND intent !== `other` (catch-all too low-confidence) AND ≥1 recorded file AND none of its files ∈ changedFiles. `changedFiles` supplied by caller (Unit 3 computes via mtime); absent ⇒ nothing changed. Pure, never throws; tolerates malformed (null/string/number) entries in the priors array.

Group 20: 19 inlined faithful-copy tests (classifier per-label + precedence + empty/non-string fallback; overlap identical/divergent/null; reuse hit on unchanged files; no-reuse on divergent intent / changed referenced file; still-reuse on unrelated changed file; empty/null priors → null; `other` excluded; file-less block excluded; skip-changed-newer-reuse-older; malformed-entry tolerance). 202 unit tests green (was 183).

(Prior — Phase 4, Unit 1: extract prior RLM blocks from transcript — commit 8206a28.) Added `extractPriorRLMBlocks(transcriptText, {window=CONFIG.contextWindow})` plus two normalizers in a new "CONVERSATION CONTEXT AWARENESS (Phase 4)" section right after `gatherConversationContext` in `rlm-hook.mjs`. **Verified the real emitted tag first:** `formatOutput` wraps a `JSON.stringify(analysis, null, 2)` body in `<rlm_preresearch>` (agentic, the default — has `intent` string + `relevant_files`) or `<rlm_analysis>` (fast: `intent` string + `files`; detailed: `intent.primary`). The extractor is **pure string-in→array-out**: global regex `/<rlm_(?:preresearch|analysis)>([\s\S]*?)<\/...>/g`, `JSON.parse` each body (skip on throw), skip non-object/array roots, push `{intent, relevant_files, raw}`. `normalizeIntent` handles string-or-`{primary}`; `normalizeRelevantFiles` flattens `relevant_files ?? files` accepting strings or `{path}` objects, dropping empties. Matches arrive oldest→newest so the array is **reversed to newest-first** then `slice(0, window)` (window≤0/non-finite ⇒ all). **Never throws.** **CRITICAL caller contract (documented in the JSDoc, matters for Unit 2/3 wiring):** pass DECODED message text, not raw JSONL bytes — inside a JSONL line the block's quotes/newlines are escaped, so `gatherConversationContext`'s existing per-line `JSON.parse` + content-decode must feed the extractor (and note its current `preview` is truncated to 200 chars — too short to hold a full block; the wiring will need the full decoded text, not `preview`). Also added `CONFIG.contextWindow` (`RLM_CONTEXT_WINDOW`, default 5) now since the function default needs it — **this folds in Unit 4's env var early**, so Unit 4 reduces to wiring the window into the look-back, not adding the flag. Group 19: 12 inlined tests (single agentic block w/ string+object files; newest-first ordering; window cap; window≤0/Infinity ⇒ all; `<rlm_analysis>` recognized; detailed `{primary}` intent; malformed body skipped but valid kept; array-root skipped; plain conversation ⇒ []; null/undefined/empty/number ⇒ []; missing intent/files degrades; empty/objectless path entries dropped). 183 unit tests green (was 171).

(Prior — Phase 3, Unit 5 `index.json` reverse index + Unit 6 test coverage — commit ce945e6. PHASE 3 COMPLETE.) Added three helpers to the SEMANTIC CACHE section (after `embeddingPath`, before `saveCacheEmbedding`): `indexPath()` → `<cacheDir>/index.json`; `readCacheIndex()` → parses index.json, returns `{}` on absent/malformed/non-object-or-array root, **never throws**; `updateCacheIndex(key, vec)` → best-effort upsert `index[key] = { dim, vec: Array.from(vec) }`, atomic tmp+rename, **gated on `CONFIG.semanticCache` (zero I/O off)**, whole body try/catch → false on failure (an index failure must never break the embedding write). **Decision taken: inline vectors** (single-read lookup) over key-list. Wired `await updateCacheIndex(key, vec)` into `saveCacheEmbedding` right after the `.embedding` rename. **Refactored `semanticLookup`** into two extracted scorers each returning `{bestKey,bestScore}|null`: `scoreFromIndex(queryVec, simImpl)` (reads index.json once, `Float32Array.from(entry.vec)`, per-entry try/catch skips foreign-dim, returns null if index empty/no-usable-vector) and `scoreFromFiles(queryVec, simImpl)` (the prior per-file `.embedding` scan, unchanged, the robust fallback). New body: embed query → `best = scoreFromIndex(...) ?? scoreFromFiles(...)` → if null log "no candidate embeddings" → threshold check → `checkImpl(bestKey)` (TTL/parse, gone→miss) → hit. `.embedding` files stay the **source of truth**, so absent/empty/corrupt index degrades to file scan; index just lags-but-self-heals (last-writer-wins race accepted per plan). Group 18: 10 inlined tests via `semanticLookupIndexed` + faithful helper copies (upsert round-trip w/ float32 tolerance; multi-key accumulate; same-key overwrite; off-gating no-write; readCacheIndex {} for absent/malformed/array; index-only nearest with ZERO `.embedding` files present; corrupt index→file-scan fallback; absent index→file-scan fallback; below-threshold miss; foreign-dim index entry skipped). **Unit 6 satisfied by existing tests:** similarity-math=Group 15, threshold-gating=Groups 17+18, index-read/write=Group 18. 171 unit tests green (was 161).

(Prior — Phase 3 Unit 4, commit dd02deb: `semanticLookup` file-scan on SHA-256 miss, zero-I/O when flag/key off, per-file + top-level try/catch, wired into `main` after SHA miss, Group 17 8 tests. Unit 3 5c76192: `embeddingPath`/`saveCacheEmbedding` raw-float32 sidecar, Group 16 6 tests. Unit 2 9c6030f: `cosineSimilarity`, length-mismatch THROWS, zero-mag→0, Group 15 7 tests. Unit 1 af5908f: 5 CONFIG flags + `embedText`→Float32Array, Group 14 8 tests. Phase 2 COMPLETE abe7313. Phase 1 COMPLETE.)

## Current phase

Phase 5 — Metrics Dashboard (Phase 4 COMPLETE — all 5 units done)

## Next unit

**Phase 5, Unit 4 (FINAL): formalize remaining test coverage, then write `.marathon/DONE`.** Unit 3 already folded in **Group 26** (10 tests over the pure `renderHTML`/`esc` exports), which satisfies the plan's "HTML render" testing. The plan's Unit-4 list ("log append, JSONL parsing, aggregation math") is ALREADY covered: log append = Group 24, JSONL parsing + aggregation math = Group 25, HTML render = Group 26. So Unit 4 is mostly a verification pass. Options for this iteration:
1. **(Optional, thin) add a tiny edge-case test or two** not yet covered — candidates: `dashboard.mjs` `parseArgs`-style flag handling (but `parseArgs`/`buildHTML`/`main` are NOT exported — they're inside the CLI section; exporting `parseArgs` purely for testability would be the only clean way, since you must NOT import the module's `main`/server). If you add a test, keep it pure (no socket, no real FS beyond a tmp dir). Don't over-engineer — the existing 272 already cover the plan's stated Unit-4 surface.
2. **Then COMPLETE Phase 5 and the whole project:** confirm `node --test test/unit.test.mjs` green (currently 272/272), run `RUN_INTEGRATION_TESTS=1 node --test test/integration.test.mjs` (should stay 15/15 — neither parse-log nor dashboard is on the hook path), then **write `/home/coding/claude-code-rlm/.marathon/DONE`** per instruction.md "Completion" with a short summary (what was built across Phases 1–5, total test count 272, remaining open questions from plan.md §160 — scratch-file path, max-turns, multi-project cache keying, cost tracking, race conditions). Commit `.marathon/DONE` + `.marathon/STATE.md`, push, stop. **All five phases will then be complete.**

Decision guidance: if no genuinely-uncovered edge case is worth a test, SKIP step 1 — do not pad the suite. Go straight to writing DONE. The bar for adding a test is "covers a real untested code path," not "more tests = better."

**Aggregate shape reminder (for the renderer):** `aggregate(records)` → `{ days: [{day,total,hits,hit_rate,skips,skip_rate,skip_reasons,errors,error_rate,events,modes,latency:{count,p50,p95,p99,min,max}}, …] (asc by day), overall: {day:'all', …same…} }`. Latency p50/p95/p99/min/max are `null` when no `latency_ms` present.

**Event taxonomy reminder (from Unit 1):** events `skip`, `cache_hit` (`source:'sha'|'semantic'`), `context_reuse`, `haiku_skip`, `complete`, `error`; canonical hit flag is the boolean `cache_hit` field. Records carry `latency_ms`, `mode`, `input_len`, and extras (`reason`, `source`, `intent`).

## Known issues / blockers

None. Note: tests inline faithful copies of hook logic (no imports from `rlm-hook.mjs`, since it calls `main()` on load) — keep new SDK tests in that style. The loop runner writes to an untracked nested `.marathon/.marathon/logs/` dir; leave it untracked, do not commit it.

## Phase completion

- [x] Phase 1 — Core Hook (COMPLETE)
- [x] Phase 2 — SDK-Direct Mode (COMPLETE)
- [x] Phase 3 — Semantic Caching (COMPLETE — Units 1-6)
- [x] Phase 4 — Conversation Context Awareness (COMPLETE — Units 1–5: extractPriorRLMBlocks + CONFIG.contextWindow; classifyIntentLocal + intentsOverlap + findReusablePriorBlock; computeChangedFiles + block `ts`; extractPriorBlocksFromTranscript + gatherConversationContext `{messages, priorBlocks}`; findReusablePriorBlockWithMtime + main() early-exit wiring.)
- [ ] Phase 5 — Metrics Dashboard (Unit 1 DONE: appendMetric + currentMode + recordMetric wired into every main() exit, Group 24. Unit 2 DONE: bench/parse-log.mjs aggregator + Group 25 (18 tests, direct import). Unit 3 DONE: bench/dashboard.mjs renderHTML + --serve + Group 26 (10 tests, direct import). Unit 4 (final): verify coverage, write `.marathon/DONE` — plan's Unit-4 test surface already covered by Groups 24/25/26.)
