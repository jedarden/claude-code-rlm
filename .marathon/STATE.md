# Marathon State — claude-code-rlm

## Last completed

**Phase 3, Unit 4 (semantic lookup on cache miss) — commit dd02deb.** Added `semanticLookup(queryText, {embedImpl=embedText, simImpl=cosineSimilarity, checkImpl=checkCache})` to the SEMANTIC CACHE section (after `saveCacheEmbedding`, before RESPONSE PARSING). Behavior: returns `null` immediately doing **zero I/O** when `!CONFIG.semanticCache` OR `!CONFIG.embedApiKey` (default-off byte-identical). Else: embed the query, `readdir(CONFIG.cacheDir)` (own try/catch → null if dir absent), iterate `*.embedding` files, read each, **guard `buf.length===0 || buf.length%4!==0`** (skip truncated), `new Float32Array(buf.buffer, buf.byteOffset, buf.length/4)`, score via `simImpl`, track best. **Per-file try/catch** so a corrupt/foreign-dim vector (cosineSimilarity throws on length mismatch) is skipped, not fatal. If `bestKey===null || bestScore < CONFIG.semanticThreshold` (0.92) → log miss, return null. Else `checkImpl(bestKey)` (reuses TTL+parse); if entry gone (expired/vanished) → log + null; else log hit + return entry. **Whole body in top-level try/catch** → any failure degrades to null (fall through to Haiku), never throws. Wired into `main` right after the SHA-256 miss (line ~1110): `const semanticHit = await semanticLookup(userMessage);` then `console.log(formatOutput(semanticHit)); process.exit(0)` on hit. **Embeds `userMessage`** to match Unit 3's stored vector. Group 17: 8 inlined tests (nearest-above-threshold returns closest of 3; all-below→null; corrupt 3-byte + 2-dim siblings skipped, good one still matches; flag-off→null no-embed; no-key→null no-embed; matched-but-JSON-gone→null; embedder throw→null; absent cache dir→null). Note: alignment is safe — Node pads buffer-pool allocs to 8 bytes, and real 1536-dim files (6144B > 4096) are unpooled at byteOffset 0. Test `writeEntry` helper coerces array→Float32Array before `.buffer`. 161 unit tests green (was 153).

(Prior — Phase 3 Unit 3, commit 5c76192: `embeddingPath`/`saveCacheEmbedding`, best-effort raw-float32 `.embedding` sidecar write gated on semanticCache, Group 16 6 tests. Unit 2, 9c6030f: pure-math `cosineSimilarity(a,b)`, length-mismatch THROWS, zero-mag → 0, Group 15 7 tests. Unit 1, af5908f: 5 CONFIG flags + `embedText(text, apiKey, {fetchImpl=fetch, model, baseUrl})` → Float32Array, throws on every failure, Group 14 8 tests. Phase 2 COMPLETE, abe7313: Unit 7 integration SDK scenarios. Unit 6 58b466a: Group 13. Unit 5 b5bfff8: agentic SDK, Group 12. Unit 4 7d1da25: detailed SDK, Group 11. Units 1-3 979e2b1: SDK v0.105.0 + fast SDK, Group 10.)

## Current phase

Phase 3 — Semantic Caching (in progress; Units 1-3 done)

## Next unit

**Phase 3, Unit 5: `cache/index.json` reverse index.** Goal (plan): a lightweight `hash → embedding path` reverse index so semantic lookup doesn't `readdir`-and-read every `.embedding` on each miss. Plan sketch calls it `index.json` in the cache dir. Build: (1) on `saveCacheEmbedding` success, upsert an entry into `<cacheDir>/index.json` (read existing JSON or `{}`, set `index[key] = { dim, mtime }` or just `index[key] = "<key>.embedding"`, atomic-write back via tmp+rename like the other writers; whole thing best-effort/try-catch so a bad index never breaks the embedding write). Consider storing the float32 vector inline in the index too, OR keep the `.embedding` files as the source of truth and use the index only to enumerate keys (avoids one big JSON blob that grows unbounded — but plan says "avoid scanning all files", and reading index.json + the listed `.embedding` files is the same number of reads… so the real win is inlining vectors into index.json to make lookup a single read). **Decision to make in Unit 5:** inline vectors in index.json (single-read lookup, but big file + rewrite-on-every-save) vs. index-as-key-list (cheap upsert, still N reads at lookup). Given cache sizes are small, **inlining vectors is the cleaner win** — `semanticLookup` reads one `index.json`, iterates entries, scores in-memory, no per-file reads. (2) Refactor `semanticLookup` to prefer index.json when present (iterate `index` entries, reconstruct Float32Array from stored array, score), falling back to the current `.embedding` file scan when index.json is absent/corrupt (keep that path as the robust fallback). (3) Add Group 18 tests: index upsert round-trip, multiple keys, corrupt index.json → falls back to file scan, lookup via index returns nearest. Files: `rlm-hook.mjs` (add `indexPath`, `updateCacheIndex`, refactor `semanticLookup`), wire `updateCacheIndex` into `saveCacheEmbedding` or `main` after it; `test/unit.test.mjs` Group 18. Then Unit 6 (already partly covered by Groups 15-17) is "similarity math correctness, threshold gating, index read/write" — fold any remaining index read/write coverage there or mark Phase 3 done if Units 1-5 + tests cover the plan's Unit-6 list.

## Known issues / blockers

None. Note: tests inline faithful copies of hook logic (no imports from `rlm-hook.mjs`, since it calls `main()` on load) — keep new SDK tests in that style. The loop runner writes to an untracked nested `.marathon/.marathon/logs/` dir; leave it untracked, do not commit it.

## Phase completion

- [x] Phase 1 — Core Hook (COMPLETE)
- [x] Phase 2 — SDK-Direct Mode (COMPLETE)
- [ ] Phase 3 — Semantic Caching (Units 1-4 of 6 done)
- [ ] Phase 4 — Conversation Context Awareness
- [ ] Phase 5 — Metrics Dashboard
